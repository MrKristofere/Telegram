@@ -392,93 +392,65 @@
           def parse_metadata(data):
               """
               Parse metadata from a standalone patch.
 
               Supported forms:
 
                   Author: John Doe <john@example.com>
                   From: John Doe <john@example.com>
                   Date: ...
                   Subject: ...
 
-              The body starts after the metadata header block.
-              Continuation lines for Subject are supported.
+              For standalone patches without an explicit Subject:
+              the first paragraph after the metadata headers is the
+              subject, and the following paragraphs form the body.
               """
 
               metadata_part, diff_part = split_patch_content(data)
 
               lines = metadata_part.splitlines()
 
               author_name = ""
               author_email = ""
               author_date = ""
               subject = ""
-
-              body_start = None
-              current_header = None
               header_re = re.compile(
                   r"^(Author|From|Date|Subject):\s*(.*)$",
                   re.IGNORECASE,
               )
 
-              for i, line in enumerate(lines):
+              i = 0
+              current_header = None
+
+              # Parse explicit metadata headers.
+              while i < len(lines):
+                  line = lines[i]
                   match = header_re.match(line)
 
                   if match:
                       key = match.group(1).lower()
                       value = match.group(2).strip()
 
                       current_header = key
                       if key == "author":
                           author_name, author_email = parse_author(value)
-
                       elif key == "from":
                           if not author_name:
                               author_name, author_email = parse_author(value)
-
                       elif key == "date":
                           author_date = normalize_date(value)
-
                       elif key == "subject":
                           subject = value
 
+                      i += 1
                       continue
+
                   # RFC/mail-style continuation lines.
                   if (
                       line[:1].isspace()
                       and current_header == "subject"
                   ):
                       subject += " " + line.strip()
+                      i += 1
                       continue
-                  # A completely blank line after headers starts body.
-                  if not line.strip() and any([
-                      author_name,
-                      author_email,
-                      author_date,
-                      subject,
-                  ]):
-                      body_start = i + 1
-                      break
-                  # Non-header text before diff is body.
-                  if (
-                      line.strip()
-                      and any([
-                          author_name,
-                          author_email,
-                          author_date,
-                          subject,
-                      ])
-                  ):
-                      body_start = i
+
+                  # First non-header line marks the end of metadata.
+                  if not line.strip():
+                      i += 1
+                      break
+
+                  if not any([
+                      author_name,
+                      author_email,
+                      author_date,
+                      subject,
+                  ]):
+                      break
+
+                  # Non-header text after metadata starts the message.
+                  break
+
+              # Remove blank lines between metadata and commit message.
+              while i < len(lines) and not lines[i].strip():
+                  i += 1
+
+              message_lines = lines[i:]
+
+              # Without an explicit Subject:, use Git-style message
+              # semantics: first paragraph is subject, remainder is body.
+              if not subject and message_lines:
+                  subject_lines = []
+                  while message_lines and message_lines[0].strip():
+                      subject_lines.append(message_lines.pop(0).strip())
+
+                  subject = " ".join(subject_lines).strip()
+
+                  while message_lines and not message_lines[0].strip():
+                      message_lines.pop(0)
+
+              body = "\n".join(message_lines).strip()
+
+              return {
+                  "author_name": author_name.strip(),
+                  "author_email": author_email.strip(),
+                  "author_date": author_date.strip(),
+                  "subject": subject.strip(),
+                  "body": body,
+                  "diff": diff_part,
+              }
-                  break
-
-              body = ""
-              if body_start is not None:
-                  body = "\n".join(lines[body_start:]).strip()
-
-              return {
-                  "author_name": author_name.strip(),
-                  "author_email": author_email.strip(),
-                  "author_date": author_date.strip(),
-                  "subject": subject.strip(),
-                  "body": body,
-                  "diff": diff_part,
-              }
